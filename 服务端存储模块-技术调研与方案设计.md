<!-- 负责人：成员2：服务端存储 -->
# 局域网网盘系统 · 服务端存储模块技术调研与方案设计

> **文档定位**：对应小组分工 **02 服务端存储**（文件存储引擎 / 数据库设计 / 文件分片与秒传）。
> 本报告可直接作为**开题报告**第 2 节"技术调研 + 方案设计 + 关键技术"，以及**结题文档**第 2 节"技术方案总结（关键技术、存储方案、技术难点及解决思路）"的底稿。
> **编写日期**：2026-08-24　**负责人**：服务端存储

---

## 目 录

0. 需求再梳理：与存储模块相关的硬性约束
1. 模块边界与团队数据接口（数据结构的协商约定）
2. 关键技术选型总览
3. 模块一：文件存储引擎
4. 模块二：数据库设计
5. 模块三：文件分片与秒传
6. 自定义二进制通信协议设计（存储相关部分）
7. 安全要点：SHA-256 密码哈希与 Token 会话
8. 磁盘配额的设计与实现
9. 开发计划映射（对齐四周迭代）
10. 文档与代码规范（对齐评分规则）
11. 参考资料

---

## 0. 需求再梳理：与存储模块相关的硬性约束

从《局域网网盘系统开发》项目要求中抽取的、**直接约束本模块**的条款如下。每一项都给出它对存储模块的含义和落地决策。

| 项目要求条款（原文要点） | 对存储模块的含义 | 落地决策 |
|---|---|---|
| C/S 架构，服务端负责"文件元数据管理、文件存储调度、并发连接处理" | 元数据与文件字节分离开来管理 | **元数据（数据库）与实体数据（文件存储引擎）分离**，全篇核心原则 |
| 通信协议："自定义二进制协议或采用 HTTP/HTTPS" | 局域网以 TCP 自定义二进制协议优先 | 本模块给出 **存储相关的报文结构**（见第 6 节），作为与"服务端架构"和"客户端通信"组成员协商的接口 |
| 用户管理：密码哈希（**SHA-256**）存储 + Token 机制 | 用户表需含哈希字段；需要会话/Token 表 | `user` 表存 SHA-256 加盐哈希；`token` 会话表（见 4.2、第 7 节） |
| 文件管理：上传/下载/删除/重命名 + **目录树结构，支持多级文件夹** | 文件不能是扁平列表，要用目录树 | `user_file` 表用 `parent_id` 表达树形结构（见 4.2） |
| 分片与秒传：**大文件分片上传 + 断点续传**，通过**文件哈希值**实现秒传 | 本模块核心；需要哈希判重 + 分片进度持久化 | `upload_session` / `upload_chunk` 表 + 内容寻址存储（见第 3、5 节） |
| 存储管理：**元信息与实体数据分离存储** + **每个用户磁盘配额** | 配额校验贯穿上传流程 | `user.quota` / `user.storage_used` + 上传各阶段校验（见第 8 节） |
| 数据存储：使用 **SQLite 或 MySQL** 存储用户信息和文件元数据，管理文件分片 | 二选一，DDL 需兼容两种 | 主推 MySQL 8，SQLite 作为零配置快速方案，两者 DDL 都给出（见 4.1） |
| 服务端并发：多线程/线程池 + **epoll IO 多路复用** | 多个客户端并发上传同一文件、同一分片合并 | 存储引擎必须**线程安全**；合并用线程池；引用计数用原子操作（见 3.6、5.4） |
| 客户端：进度显示、手动暂停/恢复、断点续传 | 服务端要能返回"已收哪些分片"，支撑进度计算 | 分片级 ACK + 会话查询接口（见 6.3、6.5） |
| 评分：结题文档 25% / 源码注释 10% / 运行录屏 35% / 打包 | 本模块代码需注释规范；文档结构清晰 | 见第 10 节文档与代码规范 |

---

## 1. 模块边界与团队数据接口（数据结构的协商约定）

开题报告明确要求"**提前规划好如何整合各部分开发内容，例如约定变量、函数或类的命名规则，协商好数据接口（数据结构）**"。作为存储组成员，你需要在开工前与另外三人敲定下面的接口，这是本模块的对外契约。

### 1.1 模块边界

```
                    ┌─────────────────────────────┐
  客户端(03/04) ───▶│ 服务端架构(01) 网络/协议/会话   │
                    └──────────────┬──────────────┘
                                   │ 调用存储模块接口（见下）
                   ┌───────────────▼───────────────┐
                   │  本模块(02) 服务端存储          │
                   │  · 数据库读写（元数据）          │
                   │  · 文件存储引擎（实体数据）      │
                   │  · 分片/秒传/断点续传逻辑        │
                   └───────┬───────────┬───────────┘
                           │           │
                      MySQL/SQLite     磁盘文件目录
```

### 1.2 需要向团队协商敲定的"数据接口"清单

| 接口项 | 协商内容 | 本报告给出 |
|---|---|---|
| 报文结构 | 命令码分配、通用帧格式、各命令的 payload 结构 | 见 6.1 / 6.3 |
| 存储引擎上层接口 | 逻辑层如何调用存储引擎（增删查引用、写入分片、合并） | 见 3.8 类骨架 |
| 目录树数据结构 | 客户端如何表示多级目录、服务端如何返回目录列表 | 见 6.3 `LIST_DIR` |
| 命名规则 | 类/函数/变量命名（建议 `camelCase` 函数 + `snake_case` 变量 + 类 `PascalCase`） | 见第 10 节 |
| 文件/目录 ID 语义 | `fid` 物理文件 ID、`ufid` 用户目录项 ID、`parent_id` 目录关系 | 见 4.2 |

---

## 2. 关键技术选型总览

| 维度 | 课程推荐（落地） | 工业级（加分/答辩参照） | 依据 |
|---|---|---|---|
| 通信协议 | **TCP 自定义二进制协议** | HTTP/HTTPS + JSON | 项目要求"局域网优先自定义二进制协议" |
| HTTP 框架（若走 HTTP） | cpp-httplib（header-only） | Drogon（性能+协程） | 2026 实测 Drogon 高并发第一梯队 |
| 数据库 | MySQL 8（多用户演示更真实） / SQLite3（零配置最快） | MySQL 8 / TiDB | 项目要求 SQLite/MySQL 二选一 |
| 存储引擎 | **自研**（本地文件系统 + 内容寻址） | MinIO / SeaweedFS / FastDFS 对照 | 自研能讲清原理、答辩有深度 |
| 哈希 | OpenSSL（MD5、SHA-1、SHA-256） | MD5+SHA1 双哈希 / 分块哈希 / BLAKE3 | 秒传判重 + 项目要求的 SHA-256 |
| 分片大小 | 4~8 MB | 4~8 MB（对齐 4KB 块） | 平衡请求开销与重传成本 |
| 并发模型 | 线程池 + `std::thread` | epoll + 线程池 / io_uring | 项目要求 epoll + 多线程 |
| 辅助 | `nlohmann/json`、`spdlog`、`gtest`、`libcurl`(客户端) | 按需扩展 | 工程化 |

**给本人的推荐一句话**：本模块最容易被低估的工作是**分片与秒传的并发一致性**（两个用户同时上传同一大文件、引用计数归零回收、断点续传会话清理），把它想清楚，答辩时就是你的亮点。

---

## 3. 模块一：文件存储引擎

### 3.1 它到底要解决哪四个问题

1. **落盘**：文件字节放哪、怎么放，能支撑海量文件、多级目录。
2. **去重**：同一内容物理上只存一份——这是秒传能成立的数据基础。
3. **完整性**：存进去的和拿出来的必须一致（处理网络传输损坏、磁盘静默损坏）。
4. **回收**：内容不再被任何用户引用时，物理空间能释放。

### 3.2 存储模型：内容寻址 + 引用计数

核心思想一句话：**文件名 = 内容哈希（content-addressed storage）**。

- 同一内容 → 相同哈希 → 落到相同路径 → **天然去重**，无需额外查重逻辑。
- 用 `ref_count`（引用计数）记录该物理对象被多少个"用户目录项"引用；为 0 时才真正物理删除。

这既满足"元信息与实体数据分离"，又是秒传与磁盘省空间的共同基础。

### 3.3 磁盘目录布局（课程可落地的最小可行方案）

```
/netdisk_data/                            # 存储根目录（可配置）
├── objects/                              # 正式对象区（内容寻址）
│   ├── 00/  01/  02/ ... ff/             # 按哈希前 2 位分桶，共 256 桶
│   │   └── a3f1c0d2...9b4f               # 文件名 = 完整内容哈希
│   └── ...
├── chunks/                               # 上传分片临时区
│   └── {upload_id}/                      #   一个上传会话一个子目录
│       ├── 0000.part
│       ├── 0001.part
│       └── ...
└── trash/                                # 回收站（延迟物理删除）
```

三个关键决策：

1. **按哈希前两位分桶**：ext4/NTFS 单目录文件数过多后，目录遍历和 open 明显变慢，分 256 桶把单目录负载降到 1/256。
2. **小文件直接存一个对象；大文件可切块存**（进阶，可选）。超过阈值（如 64MB）切成固定块分开存放，降低复制/校验/回收成本。参考 SeaweedFS 的 volume/needle 和淘宝 TFS 的"小文件合并进大文件"。
3. **删除走回收站**：删除时把对象移入 `trash/`（或 `objects` 内打标记），后台线程定期物理清理，避免阻塞主链路、避免误删。

### 3.4 写入 / 删除 / 回收流程

```
【写入（首次落盘）】
  分片到齐 → 线程池内按 chunk_index 顺序拼接成临时文件
  → 对拼接结果重算整体 MD5/SHA-1，与客户端上报值比对（防传输损坏）
  → 通过 → 按整体哈希定位 objects/{前2位}/{hash}
      若已存在（并发同传命中）→ 复用，丢弃临时文件
      否则 → rename 到目标路径（原子）
  → 写 file 表 + 写 user_file 表（同一事务）
  → 清理 chunks/{upload_id}/

【删除】
  删除 user_file 行（用户视角）→ file.ref_count - 1（原子）
  → 若 ref_count 归零 → 把对象 move 到 trash/ → 后台线程物理删除

【回收（后台 GC）】
  定时扫描 upload_session（expire_at 超时）→ 清理孤儿分片
  定时扫描 trash/ → 物理删除
```

### 3.5 数据完整性

| 场景 | 对策 |
|---|---|
| 传输中损坏 | 分片写入时校验分片哈希；合并后对整文件重算哈希与上报值比对，不一致整体作废重传 |
| 磁盘静默损坏 | 对象旁存放 `.crc`（CRC32，廉价），读取时抽验；工业级用纠删码（MinIO 的 Reed-Solomon），课程不必 |
| 秒传后文件损坏 | 前提是"已存在对象的哈希可信"，所以**首次入库必须整文件校验**，保证 file 表哈希永远可信 |

**文件 I/O 建议**：大文件用 `pread/pwrite`（线程安全、可并发）；读吞吐敏感可用 `mmap`；Linux 5.1+ 可上 `io_uring`（课程够不着可不做）。Windows 用 `CreateFile` + `OVERLAPPED`。

### 3.6 并发处理（对"epoll + 线程池"的呼应）

- **线程池合并分片**：完成一个大文件需要拼接多个分片，这是 CPU/IO 密集任务，交给线程池，避免逐个请求串行。
- **`std::atomic` 引用计数**：多个请求并发对同一物理文件 +1/-1，用原子操作；归零判定用事务 + `SELECT ... FOR UPDATE` 或 CAS，防止并发删除把 ref_count 减成负数、把文件重复物理删掉。
- **加锁粒度**：存储引擎内部对"同一对象的落盘"加互斥（可用 `std::shared_mutex`，读多写少）；对不同对象尽量不加全局锁，保证吞吐。
- **存储引擎线程安全**：所有对 `objects/`、`chunks/`、`trash/` 的访问都要考虑多线程并发。因为 epoll 模型下同一连接/不同连接的处理可能落在不同线程。

### 3.7 对标系统（答辩素材）

| 系统 | 值得学的点 |
|---|---|
| **SeaweedFS** | 现代对象存储里架构最简洁：master 管元数据、volume 管数据、O(1) 磁盘寻道、Filer 提供目录语义 |
| **FastDFS** | 国内老牌：tracker（调度）+ storage（存储）双角色，文件 ID 内嵌分块信息 |
| **MinIO** | S3 兼容 + 纠删码，单机部署简单，可作为"成熟方案对照" |
| **淘宝 TFS** | 海量小文件合并存储、块设备级管理，文件系统层面思路 |

### 3.8 C++ 落地要点 + 核心类骨架

```cpp
// 命名空间统一前缀，团队约定（见第 10 节）
namespace ndisk::storage {

// 哈希类型（16B MD5 / 20B SHA-1）
using Md5  = std::array<uint8_t, 16>;
using Sha1 = std::array<uint8_t, 20>;

// 物理文件元数据（对应 file 表，仅元数据，不含文件字节）
struct FileMeta {
    uint64_t  fid;
    Md5       md5;
    Sha1      sha1;
    uint64_t  size;
    std::string storage_path;      // objects/xx/<hash>
    int       chunk_count;
    std::atomic<int64_t> ref_count;
};

// 存储引擎：对上层逻辑层暴露的统一接口
class StorageEngine {
public:
    // 秒传判定：命中返回 fid，未命中返回 0
    uint64_t lookup(const Md5& md5, const Sha1& sha1, uint64_t size);

    // 建立/释放引用：给某物理文件 ref_count ±1（原子）
    //   秒传命中时 +1；删除用户文件时 -1
    bool update_reference(uint64_t fid, int64_t delta);

    // 写入一个分片（落盘到 chunks/{upload_id}/{idx}.part），返回是否成功
    bool write_chunk(const std::string& upload_id, int idx,
                     const char* data, size_t len);

    // 合并分片为整文件 → 内容寻址落盘；若已存在则复用并返回其 fid
    //   内部校验整文件哈希；成功返回 fid，失败返回 0
    uint64_t merge_and_store(const std::string& upload_id, int chunk_count,
                             const Md5& md5, const Sha1& sha1);

    // 打开某物理文件读取（下载），返回文件描述符
    int open_read(uint64_t fid);

    // 清分片/清回收站（后台线程调用）
    void cleanup_expired();
};
}  // namespace ndisk::storage
```

> 说明字段：`fid`（物理文件，全系统唯一）、`ufid`（用户目录项，见下节）。**区分这两者是整个文件管理的关键**：一个物理文件可对应多个用户目录项，用户改名不改物理对象、用户重名不影响物理去重。

---

## 4. 模块二：数据库设计

### 4.1 选型：SQLite vs MySQL

| 维度 | SQLite3 | MySQL 8 |
|---|---|---|
| 部署 | 零配置、单文件、无需装服务 | 需装服务端，较繁琐 |
| 并发写 | **单写者锁**，多用户同时上传会成为瓶颈 | 行级锁、并发好，适合多用户演示 |
| 真实感（答辩） | 偏"作业" | 更接近生产、更好讲"多用户秒传去重" |
| 适合本模块 | 单机、快速出功能 | **推荐**，能体现多用户并发秒传 |

**建议**：主推 **MySQL 8**（多用户并发秒传有说服力），SQLite 作为零配置快速方案。下方 DDL 以 MySQL 为标准，SQLite 差异见 4.2 末尾的映射说明。

> 铁律：**数据库里绝不放文件字节**，只放元数据。文件字节全部交给存储引擎。

### 4.2 核心表设计（MySQL DDL）

分层的逻辑：`file`（物理内容，全系统一份）+ `user_file`（每个用户的目录项，可重名）→ 这是秒传"多个用户共用同一物理文件"的关键。**元数据与实体数据分离**在此体现为：`file.storage_path` 指向存储引擎里的对象，字节永远不落在数据库。

#### ① 用户表 `user`

```sql
CREATE TABLE `user` (
  `uid`           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `username`      VARCHAR(64)  NOT NULL,
  `salt`          CHAR(16)     NOT NULL,             -- 每用户随机盐（见第 7 节）
  `password_hash` CHAR(64)     NOT NULL,             -- SHA-256(salt + password) 的十六进制
  `storage_used`  BIGINT UNSIGNED NOT NULL DEFAULT 0, -- 该用户已用空间（用户视角）
  `quota`         BIGINT UNSIGNED NOT NULL DEFAULT 10737418240, -- 默认 10GB 配额
  `status`        TINYINT      NOT NULL DEFAULT 1,   -- 1正常 0冻结
  `created_at`    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`uid`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### ② 会话表 `user_token`（对应用户管理里的 Token 机制）

```sql
CREATE TABLE `user_token` (
  `id`         BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `uid`        BIGINT UNSIGNED NOT NULL,
  `token`      CHAR(64) NOT NULL,                    -- 登录后下发的随机令牌（十六进制）
  `expire_at`  DATETIME NOT NULL,                    -- 会话过期时间
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_token` (`token`),
  KEY `idx_uid` (`uid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### ③ 物理文件表 `file`（同一内容全系统只有一行 —— 秒传的数据基础）

```sql
CREATE TABLE `file` (
  `fid`          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `md5`          CHAR(32) NOT NULL,                  -- 整文件 MD5（十六进制）
  `sha1`         CHAR(40) NOT NULL,                  -- 整文件 SHA-1（双哈希降碰撞）
  `size`         BIGINT UNSIGNED NOT NULL,
  `storage_path` VARCHAR(512) NOT NULL,              -- 存储引擎中的对象路径
  `chunk_count`  INT UNSIGNED NOT NULL DEFAULT 1,    -- 分片数（小文件为 1）
  `ref_count`    BIGINT UNSIGNED NOT NULL DEFAULT 1, -- 引用计数：多少用户目录项引用它
  `created_at`   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`fid`),
  UNIQUE KEY `uk_hash` (`md5`, `size`)               -- ★秒传判定核心唯一索引
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### ④ 用户目录项表 `user_file`（每个用户看到一套自己的文件树）

```sql
CREATE TABLE `user_file` (
  `ufid`       BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `uid`        BIGINT UNSIGNED NOT NULL,
  `parent_id`  BIGINT UNSIGNED NOT NULL DEFAULT 0,   -- 目录树父节点，0=根
  `name`       VARCHAR(255) NOT NULL,                -- 用户自己的文件名（可重名！）
  `is_dir`     TINYINT NOT NULL DEFAULT 0,           -- 0=文件 1=目录
  `fid`        BIGINT UNSIGNED DEFAULT NULL,         -- 是文件时 → file.fid
  `size`       BIGINT UNSIGNED NOT NULL DEFAULT 0,
  `status`     TINYINT NOT NULL DEFAULT 1,           -- 1正常 2回收站 3已删除
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`ufid`),
  KEY `idx_uid_parent` (`uid`, `parent_id`)          -- 列目录 / 遍历目录树
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### ⑤ 上传会话表 `upload_session`（断点续传的进度中枢）

```sql
CREATE TABLE `upload_session` (
  `upload_id`    CHAR(36) NOT NULL,                   -- UUID
  `uid`          BIGINT UNSIGNED NOT NULL,
  `md5`          CHAR(32) NOT NULL,                   -- 整文件哈希（秒传复检用）
  `sha1`         CHAR(40) NOT NULL,
  `size`         BIGINT UNSIGNED NOT NULL,
  `chunk_size`   INT UNSIGNED NOT NULL,               -- 每片字节数
  `total_chunks` INT UNSIGNED NOT NULL,
  `status`       TINYINT NOT NULL DEFAULT 0,          -- 0上传中 1待合并 2完成 3已过期
  `expire_at`    DATETIME NOT NULL,                   -- 超时清理孤儿分片
  `created_at`   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`upload_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### ⑥ 分片进度表 `upload_chunk`（每片一行：谁传过、传的对不对）

```sql
CREATE TABLE `upload_chunk` (
  `upload_id`    CHAR(36) NOT NULL,
  `chunk_index`  INT UNSIGNED NOT NULL,               -- 从 0 开始
  `chunk_md5`    CHAR(32) NOT NULL,                   -- 单分片校验
  `chunk_size`   INT UNSIGNED NOT NULL,
  `storage_path` VARCHAR(512) NOT NULL,               -- chunks/{upload_id}/{index}.part
  `created_at`   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`upload_id`, `chunk_index`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### ⑦ 分享表 `share`（扩展功能，顺手可做）

```sql
CREATE TABLE `share` (
  `share_id`  CHAR(8)      NOT NULL,                  -- 短链接 ID
  `ufid`      BIGINT UNSIGNED NOT NULL,
  `uid`       BIGINT UNSIGNED NOT NULL,
  `code`      CHAR(4)      DEFAULT NULL,              -- 提取码
  `status`    TINYINT      NOT NULL DEFAULT 1,
  `created_at`DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`share_id`),
  KEY `idx_ufid` (`ufid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### SQLite 适配说明（若选 SQLite3）

| MySQL 写法 | SQLite 等价写法 |
|---|---|
| `BIGINT UNSIGNED NOT NULL AUTO_INCREMENT` | `INTEGER PRIMARY KEY AUTOINCREMENT` |
| `DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`（或存 `TEXT` + `strftime`） |
| `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4` | 去掉（SQLite 无此语法） |
| `CHAR(32)` / `CHAR(40)` | 可直接用 `CHAR`，或统一用 `TEXT`（SQLite 动态类型，`TEXT` 更省心） |
| `ON UPDATE CURRENT_TIMESTAMP` | 不支持，需在应用层维护 `updated_at` |

> SQLite 注意：**单写者锁**，高并发多用户同时上传会排队。若要演示"多个用户同时秒传同一文件"，MySQL 更稳。

### 4.3 索引设计要点

- **`uk_hash (md5, size)` 是秒传的命脉**：秒传时一条 `SELECT ... WHERE md5=? AND size=?` 走唯一索引，命中即"内容已存在"，O(1) 判定。**必须把 `size` 也纳入索引**——只靠 MD5 理论上有碰撞，加上 size 后误判概率低到可忽略。
- `idx_uid_parent (uid, parent_id)` 支撑"打开文件夹/遍历目录树"。
- 若要按文件名搜索，可加 `(uid, name)` 联合索引，或新增一列 `name_lower` 做前缀搜索。
- 分片进度表用 `(upload_id, chunk_index)` 复合主键，天然支持"查某会话已收到哪些分片"的区间查询。

### 4.4 事务与一致性（本模块最容易翻车的地方）

**场景 A：秒传的原子性。** 命中已存在的物理文件后，"`file.ref_count + 1` + 插入 `user_file` 行"必须在一个事务里完成，否则出现"引用加了但用户文件没建"的孤儿。

**场景 B：上传完成的原子性。** 合并好物理文件后，"写 `file` 行 + 写 `user_file` 行"用事务保证。更稳做法是**幂等键**（用 `upload_id` 唯一标识），即使客户端重复提交 complete 也不会产生两份。

**场景 C：并发同传同一文件。** 两个用户同时上传同一内容，A 先合并完成写了 `file` 行，B 的 complete 查询命中 → 走秒传逻辑即可，无需二次落盘。合并线程池要考虑这点：**谁先完成谁落盘，后到者直接复用**。

**场景 D：引用计数并发。** 用 `UPDATE file SET ref_count = ref_count + 1 WHERE fid=?`（原子自增）；归零判定用事务 + `SELECT ... FOR UPDATE` 或 CAS，避免两个删除请求把 ref_count 减成负数把文件物理删了。

### 4.5 扩展优化（答辩加分项）

- **秒传查重缓存**：`md5+size → fid` 是高频只读查询，可用 Redis 缓存，命中免查 DB。
- **布隆过滤器**（工业级）：上亿文件时秒传查重不能全走 DB，先在内存过滤器里大概率判"不存在"，减少 DB 压力。
- **分表**：`file` 按 `md5` 前几位分区，`user_file` 按 `uid` 分片（量级到了再考虑，课程可不做）。

---

## 5. 模块三：文件分片与秒传

> 注意：本节与第 3 节存储引擎、第 4 节数据库是联动的——分片与秒传是"逻辑"，存储引擎是"数据落地"，数据库是"状态记录"。

### 5.1 秒传原理

一句话：**内容寻址去重**。服务端记录每个物理文件的哈希，上传前先报哈希——系统里已有同哈希同大小的文件 → 不用传数据，直接给你目录建一条引用，返回"上传成功"。

```
客户端 → 计算整文件 MD5/SHA-1 + 每个分片 MD5
客户端 → 发送 UPLOAD_INIT {md5, sha1, size, name}
   服务端查 file 表（md5, size）唯一索引：
   ├─ 命中 → 事务里 ref_count+1、插入 user_file，回复 instant=1（没传一个字节）
   └─ 未命中 → 建 upload_session，回复 upload_id + chunk_size + total_chunks + 已收分片列表
```

哈希策略分层（从课程到工业级）：

| 级别 | 方案 | 说明 |
|---|---|---|
| 入门 | MD5 + size | 唯一索引判定，够用，先做这个 |
| 进阶 | MD5 + SHA-1 双哈希 | 碰撞概率降到约 2^-96，`file` 表两个字段都建唯一索引 |
| 工业级 | 分块哈希（整文件 + 固定分块序列） | 大文件算多个 4MB 分块的哈希，可只传缺失分块、服务端复检 |
| 工业级+ | 布隆过滤器内存预判 | 上亿文件时减少 DB 查询 |

### 5.2 分片上传完整时序（含协议消息交互）

```
1. 客户端：分片（默认 4~8MB，最后一片可不满）→ 算整文件哈希 + 每片哈希
2. 客户端 → 服务端：UPLOAD_INIT_REQ {md5,sha1,size,name}
3. 服务端查 file 表：
   - instant=1：秒传命中，事务建引用，回复 INSTANT_OK
   - 否则：建 upload_session，回复 {upload_id, chunk_size, total_chunks, received[]}
4. 客户端 → 服务端：CHUNK_UPLOAD_REQ {upload_id, chunk_index, chunk_md5, chunk_bytes}
   （可多线程并发 3~5 片同时传）
   服务端：校验分片哈希 → 落盘 chunks/{upload_id}/{idx}.part → 写 upload_chunk
          → 回复 CHUNK_ACK（带累计已收片数，供客户端算进度）
5. 断点续传：客户端重新 UPLOAD_INIT（或 QUERY_SESSION），拿到 received[]，只传缺失片
6. 全部到齐 → 客户端 → 服务端：COMPLETE_REQ {upload_id, md5, sha1, size, name}
   服务端：
   a. 线程池按 chunk_index 顺序拼接分片 → 临时文件
   b. 对临时文件重算 MD5/SHA-1，与上报值比对（防传输损坏）
   c. 按整体哈希定位 objects/{前2位}/{hash}，已存在则复用、删临时文件；否则 rename 落盘
   d. 事务：写 file 行 + 写 user_file 行
   e. 清理 chunks/{upload_id}/，upload_session 置完成
   f. 回复 COMPLETE_OK
7. 客户端收到 COMPLETE_OK，上传结束
```

### 5.3 断点续传

- 进度持久化在 `upload_session` + `upload_chunk` 表，服务端重启不丢进度。
- 客户端重新 init 时服务端返回 `received[]` 分片序号，客户端跳过已传分片。
- 服务端定时清理 `expire_at` 过期的会话及其孤儿分片（防磁盘被半截上传占满）。
- 协议可参考开源 **tus resumable upload protocol**（现为 IETF 草案 `draft-ietf-httpbis-resumable-upload`），它定义了 POST 创建 + PATCH 续传 + 服务端返回已收长度的完整模型；自己写自定义协议也行，但 tus 思路值得借鉴。

### 5.4 进度显示与手动暂停/恢复（对接客户端）

- **进度计算**：服务端每次 CHUNK_ACK 返回"已收片数/总片数"，客户端据此画进度条。
- **暂停/恢复**：客户端在本地停止发送分片即可"暂停"；恢复时重发 `QUERY_SESSION` 拿最新 `received[]` 续传。**服务端无需额外状态**，天然支持。

### 5.5 关键问题与对策

| 问题 | 对策 |
|---|---|
| **分片多大** | 4~8MB 最平衡：太小→请求开销大，太大→网络失败重传成本高。磁盘对齐 4KB |
| **大文件哈希算得慢** | 整文件哈希是秒传前置成本。客户端可多线程并行分块算再归并；服务端合并后用 xxHash/BLAKE3（非加密，快一个量级）复检 |
| **分片并发乱序** | 服务端按 `chunk_index` 落盘、合并时按序号拼接，天然抗乱序，无需客户端保序 |
| **哈希碰撞** | MD5 128bit 需约 2^64 数据量才有实际碰撞风险，配合 size 可忽略；求稳用双哈希 |
| **引用计数归零回收** | 物理删除放后台：先移 trash/ 再物理删；`ref_count` 用原子 UPDATE，删除用事务+CAS |
| **秒传后文件损坏** | 首次入库务必整文件校验（服务端重算哈希比对），保证 file 表哈希永远可信 |
| **并发同传同一文件** | 谁先合并落盘谁负责，后到者复用（见 5.4 场景 C） |

---

## 6. 自定义二进制通信协议设计（存储相关部分）

> 这是与"服务端架构"和"客户端通信"组成员**协商数据接口**的核心产出。设计原则：定长报头 + 变长 payload，用长度字段解决 TCP 粘包/半包。

### 6.1 通用帧格式（定长 16 字节头）

```
 0        1        2        3        4        5        6        7
+--------+--------+--------+--------+--------+--------+--------+--------+
| magic  | ver    | cmd    | flags  |  seq (uint32, 大端)                |
+--------+--------+--------+--------+--------+--------+--------+--------+
| user_id (uint32, 大端) |  length (uint32, 大端, payload 长度)          |
+--------+--------+--------+--------+--------+--------------------------+
payload  : 紧跟 header，恰好 length 字节
```

| 字段 | 大小 | 含义 |
|---|---|---|
| magic | 2B | 固定 `0x4E44`（'N''D'），用于快速纠错、区分无关数据 |
| ver | 1B | 协议版本，`0x01` |
| cmd | 1B | 命令码（见 6.2） |
| flags | 1B | 位标志（如 `0x01` 带 Token、`0x02` 是否压缩等） |
| seq | 4B | 请求序号，客户端自增；服务端响应时回填，用于配对 |
| user_id | 4B | 登录后的用户 ID（配合 Token 校验身份） |
| length | 4B | payload 字节数（**大端**，也叫网络字节序） |

> **防粘包/半包**：接收端先用 `read` 读满 16 字节头，解析 `length`，再精确读 `length` 字节的 payload。一条消息 = 头部 + 恰好 length 字节的 body。这是 TCP 二进制协议必须处理的点。

### 6.2 命令码分配表（存储相关为主，其余可扩展）

| cmd | 名称 | 方向 | 说明 |
|---|---|---|---|
| 0x01/0x02 | LOGIN_REQ/RSP | 客户端↔服务端 | 登录（用户认证，属 01，但结构需协商） |
| 0x03/0x04 | REGISTER_REQ/RSP | 客户端↔服务端 | 注册（SHA-256 加盐存哈希，见第 7 节） |
| 0x05/0x06 | UPLOAD_INIT_REQ/RSP | 客户端→服务端 | 秒传检查 + 建上传会话 |
| 0x07/0x08 | CHUNK_UPLOAD_REQ/RSP | 客户端→服务端 | 上传单个分片 |
| 0x09/0x0A | COMPLETE_REQ/RSP | 客户端→服务端 | 合并分片、校验、写元数据 |
| 0x0B/0x0C | QUERY_SESSION_REQ/RSP | 客户端→服务端 | 查询已收分片（断点续传/进度） |
| 0x0D/0x0E | DOWNLOAD_REQ/RSP | 客户端→服务端 | 下载文件 |
| 0x0F/0x10 | MKDIR_REQ/RSP | 客户端→服务端 | 新建目录 |
| 0x11/0x12 | DELETE_REQ/RSP | 客户端→服务端 | 删除文件/目录（回收站） |
| 0x13/0x14 | RENAME_REQ/RSP | 客户端→服务端 | 重命名 |
| 0x15/0x16 | LIST_DIR_REQ/RSP | 客户端→服务端 | 列目录 / 取目录树 |
| 0x17/0x18 | QUOTA_REQ/RSP | 客户端→服务端 | 查询/刷新配额与已用空间 |

### 6.3 存储相关消息结构（payload 定义）

所有多字节整数/字符串大端；字符串用 `[uint16 len][bytes]` 方式（TLV 或定长均可，团队约定统一）。

**UPLOAD_INIT_REQ (0x05)**
```
md5[16]      -- 整文件 MD5 二进制
sha1[20]     -- 整文件 SHA-1 二进制
size   uint64
name   string
```
**UPLOAD_INIT_RSP (0x06)**
```
instant  uint8   -- 1=秒传命中
upload_id[16]    -- 会话 UUID（二进制）
chunk_size  uint32
total_chunks uint32
received_count uint16  -- 已收片数（断点续传用）
received  [uint16 * received_count]  -- 已收分片下标列表
```

**CHUNK_UPLOAD_REQ (0x07)**　`payload = [chunk_index uint32][chunk_md5[16]][chunk_bytes...]`
```
chunk_index  uint32   -- 分片序号
chunk_md5    [16]     -- 该分片哈希
chunk_bytes  ...      -- 二进制内容（由 header.length 决定长度）
```
**CHUNK_UPLOAD_RSP (0x08)**
```
chunk_index  uint32
result       uint8    -- 0=OK 1=校验失败 2=配额不足等
received_count uint16 -- 累计已收片数（客户端据此算进度）
```

**COMPLETE_REQ (0x09)**
```
upload_id[16]
md5[16]  sha1[20]  size uint64  name string
```
**COMPLETE_RSP (0x0A)**
```
result uint8   -- 0=OK 其他=错误码
fid    uint64  -- 成功时的物理文件 ID
```

**QUERY_SESSION_REQ (0x0B)**　`upload_id[16]`（或 `md5[16]+size`）
**QUERY_SESSION_RSP (0x0C)**　结构与 `UPLOAD_INIT_RSP` 一致（无 instant 字段亦可）。

**LIST_DIR_RSP (0x16)**　返回某目录下条目列表（供客户端渲染目录树）：
```
entry_count uint16
[ { is_dir uint8, id uint64, parent_id uint64, name string, size uint64 } * entry_count ]
```

> **给团队的接口约定说明**：这些结构一旦确定，`01`（网络层只管收发不关心语义）、`03`（客户端按此收发）、`02`（你按此实现秒传/分片逻辑）就各自可独立开发，最后靠**同一份结构定义**拼接（可用一个头文件 `protocol.h` 集中定义，或用 protobuf/idl 生成，但课程建议手写头文件即可，注释要规范——因为占 10% 的分）。

---

## 7. 安全要点：SHA-256 密码哈希与 Token 会话

项目要求明确"采用密码哈希（**SHA-256**）安全存储，并通过 **Token 机制**管理用户会话"。遵守这一点，同时注意正确用法：

### 7.1 SHA-256 的正确用法（不要裸哈希）

- **必须加盐**：每个用户生成随机 `salt`（16 字节），存储 `SHA-256(salt + password)`。裸 `SHA-256(password)` 会被彩虹表秒破。
- **可迭代**（进阶）：对结果再迭代若干次（如 10000 次）以拖慢暴力破解。课程若只要求 SHA-256，至少做到"加盐"。
- **更优替代**（答辩可提）：bcrypt / argon2 是专业口令哈希，天生带盐和迭代；但课程指定 SHA-256，按它做并说明"已加盐 + 选做迭代"是合理折中。
- **绝不存明文**：数据库中只有 `salt` + `password_hash`。

### 7.2 Token 会话

- 登录成功后：生成随机 token（64 字节十六进制），写入 `user_token` 表并设 `expire_at`（如 24 小时），下发给客户端。
- 后续请求：在帧头 `flags` 置位 + payload 带上 token（或帧头预留字段），服务端校验 `user_token` 是否存在且未过期。
- **校验时机**：任何涉及上传/下载/删除/列目录的存储操作前，都要校验 token 与 `user_id` 匹配，防止越权访问他人文件。

---

## 8. 磁盘配额的设计与实现

"为每个用户设置磁盘配额，进行有效的存储资源管理。"

### 8.1 配额的语义（先想清楚）

| 口径 | 含义 | 建议 |
|---|---|---|
| **逻辑配额（用户视角）** | `user_file` 下所有文件大小之和（`sum(size)`），秒传也计入（用户空间确实被这个文件占了） | **推荐**，符合直觉 |
| 物理配额 | 该用户创造的物理对象大小之和（去重后），秒传不占新空间 | 作为磁盘真实占用参考 |

建议用**逻辑配额**：用户在界面上看到的"剩余空间"等于"自己所有文件加起来的大小"，秒传也占用空间（这是真实网盘的体验）。物理层靠全局去重天然省磁盘，两者并行不冲突。

### 8.2 各阶段校验点

| 阶段 | 校验 |
|---|---|
| 注册 | 初始化 `quota`（如 10GB）、`storage_used=0` |
| UPLOAD_INIT | 若目标文件将新增 `size` 且 `storage_used + size > quota` → 拒绝（返回配额不足错误码） |
| 秒传命中 | 同样占用逻辑配额（因为用户目录多了这个文件），`storage_used += size` 仍要在事务里做 |
| COMPLETE | 实际占用按 `size` 入账；去重复用对象时，`storage_used += size`（逻辑口径），物理不重复占盘 |
| 删除/回收站彻底删除 | `storage_used -= size` |

> 用 `UPDATE user SET storage_used = storage_used + ? WHERE uid=? AND storage_used + ? <= quota`，**一条原子 SQL 同时实现"累加 + 防超限"**，比"先查再改"更安全（避免并发下超配额）。

---

## 9. 开发计划映射（对齐四周迭代）

| 周 | 项目交付物 | 本模块本阶段要交付的内容 |
|---|---|---|
| **第 1 周** | 设计文档 + 可编译空项目框架 | 敲定协议报文结构（第 6 节）落成 `protocol.h`；确定数据库表结构并写好建表 SQL；**与团队定**命名规则、数据接口 |
| **第 2 周** | 单文件上传下载端到端 Demo | 完成 `user`/`user_file`/`file` 三表 + 内容寻址落盘；单文件（不切片）上传、下载、删除、重命名、目录树；SHA-256 加盐注册登录 + Token |
| **第 3 周** | 功能完整 Alpha 版 | 分片上传（init/chunk/complete 三接口）+ 并发上传 + **秒传**（先 MD5+size）+ **断点续传** + 进度返回；`upload_session`/`upload_chunk` 表 + 过期清理；配额校验 |
| **第 4 周** | 最终版 + 文档 + 答辩演示 | 集成测试、Bug 修复、性能优化；双哈希/引用计数回收/回收站；补充结题文档的"关键技术 + 技术难点及解决思路"；录屏演示秒传、断点续传、多用户并发 |

> **建议先做单文件、再加秒传、最后并发优化**——把链路先跑通（第 2 周 Demo），否则分片 + 并发 + 秒传一起上容易出 bug 且难定位。

---

## 10. 文档与代码规范（对齐评分规则）

评分结构：**结题文档 25% / 源码注释 10% / 运行录屏 35% / 打包 10%**。本模块直接关系到前两项。

### 10.1 命名规则（团队统一，开题报告第 3 点要求）

| 对象 | 规则 | 示例 |
|---|---|---|
| 命名空间 | 全小写，`项目名::模块名` | `ndisk::storage` |
| 类 / 结构体 | PascalCase | `StorageEngine`, `FileMeta` |
| 函数 | camelCase | `merge_and_store` / `writeChunk`（二选一，团队定一个） |
| 成员变量 | snake_case | `upload_id`, `chunk_index` |
| 常量/enum | 全大写加下划线 | `UPLOAD_INIT_REQ`, `MD5_LEN` |
| 数据库表/列 | snake_case 小写 | `user_file`, `parent_id` |

### 10.2 注释规范（占 10%，务必认真）

- **每个头文件**顶部：模块用途、作者、日期。
- **每个公开函数**：一句用途 + 参数含义 + 返回值 + 可能抛的异常。
- **不写赘述代码逻辑的废话注释**，但**关键业务点**（如"`uk_hash` 是秒传判定核心""这里的原子 UPDATE 防并发超配额""move 到 trash 再物理删是为了不阻塞主链路"）用一句话点出"为什么"。
- 协议结构字段逐个注释（`protocol.h` 里每条消息字段写清含义），这也是团队协作的关键。

### 10.3 建议在结题文档里如何呈现本模块

- **技术方案总结**：用第 3/4/5 节的图 + 表（存储引擎流程图、ER 关系、秒传时序）。
- **关键技术**：内容寻址去重、分片并发一致性、引用计数回收、断点续传会话管理。
- **技术难点及解决思路**：挑 2~3 个真实踩过的坑（如"两个用户并发同传同一大文件的竞争""SQLite 单写锁"，以及你的解决思路），这比罗列功能更能拿分。

---

## 11. 参考资料

**架构与秒传**
- [Design a file storage service like Google Drive（系统设计教学）](https://www.educative.io/blog/google-drive-system-design)
- [网盘文件去重与秒传实现：内容寻址 + 布隆过滤器 + 一致性哈希](https://datasea.cn/go0731659563.html)
- [网盘秒传技术原理与实践（CSDN）](https://blog.csdn.net/weixin_35238815/article/details/148671756)
- [百度网盘秒传原理分析（CSDN）](https://blog.csdn.net/bingjie1217/article/details/20690241)

**存储引擎**
- [分布式文件存储 MinIO / SeaweedFS / FastDFS 对比](https://zollty.blog.csdn.net/article/details/108463141)
- [深度揭秘淘宝自主研发的文件系统 TFS](https://www.doit.com.cn/p/61904.html)
- [SeaweedFS（源码，架构简洁可学）](https://github.com/seaweedfs/seaweedfs)
- [DeepSeek 3FS Chunk Engine](https://gitlab.deepseek.com/3fs/chunk_engine)

**上传协议**
- [tus resumable upload protocol（断点续传标准协议）](https://raw.githubusercontent.com/tus/tus-resumable-upload-protocol/0931fa48179d67cee8f7c454889d1faf5e0f92f9/protocol.md)
- [IETF resumable upload 草案](https://datatracker.ietf.org/doc/html/draft-tus-httpbis-resumable-uploads-protocol-00)

**C++ 框架 / 工程化**
- [C++ Web 框架性能实测：Hical vs Drogon vs Crow vs Oat++ vs cpp-httplib vs Cinatra（2026）](https://purecpp.cn/article.html?slug=MdtEonjE)
- [C++ Web 框架 Benchmark](https://blog.csdn.net/qq_40273354/article/details/161144518)
